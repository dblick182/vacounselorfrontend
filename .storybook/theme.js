import { create } from '@storybook/theming';
import { themes } from '@storybook/theming';

export const theme = create({
  base: themes.dark,

  brandTitle: `PLGTeamsApp.UI`,
  brandUrl:
    'https://worldwidelearning.visualstudio.com/LearningEcosystemServices/_git/PLGTeamsApp.UI?path=%2F&version=GBmaster&_a=contents',
});
